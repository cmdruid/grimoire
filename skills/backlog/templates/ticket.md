---
type: ticket
id: TK-<YYYY-MM-DD>-<slug>          # derived from the filename; stated for grep-ability
status: open                        # open | answered | resolved
subject_kind: <note|task|issue|bug|feedback>   # REQUIRED — one of the five capture kinds
origin: <entry-id>                  # promoted tickets only; DELETE this line on a direct ticket
blocking: []                        # optional; gates THIS ticket's resolution only
updated: <YYYY-MM-DD>
---

# TK-<YYYY-MM-DD>-<slug> — <subject>

## Context

<What led here — the work, the entry (link the origin entry / report where one exists), and the
constraint that makes this the human's call.>

## Decision needed

<The question, stated so a one-line answer resolves it — with the agent's recommended answer and
why.>

## Comments

<Append-only. Imported mirror comments are keyed by remote comment ID; local notes are dated.>

## Resolution

<Filled at close: the answer, the outcome (resolve / wontfix / demote), and what was done with the
origin entry.>
