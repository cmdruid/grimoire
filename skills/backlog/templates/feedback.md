### F-<n> · <short title> · YYYY-MM-DD

<F-<n> = next free counter, trunk-allocated; write ((pending: <slug>)) in the ID position
branch-side. The observation / suggestion / what worked or didn't — keep it short. Note whether
it's positive, a concern, or a directional idea, and where it might lead if anywhere (a tracker
item it should become, or "just an observation").>
