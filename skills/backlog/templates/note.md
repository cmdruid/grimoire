---
type: note
id: <N-n — next free counter, trunk-allocated; pending until curation stamps it branch-side>
status: evergreen
updated: <YYYY-MM-DD>
related: [<the index entry this backs>]
---

# <Title> — note

_backs: <the tracker entry that links here — a MEMORY line, a BACKLOG/ISSUES item, a bug report, a task>_

<The long-form context that didn't fit a single line on the index entry: the rationale, a worked
example, the design reasoning that isn't an ADR, the detail behind a fact. A `notes/` file is
*subordinate* — it exists only to be reached through the link above, never by browsing. If this
reads as a standalone investigation someone would open on its own, it belongs in `reports/`, not
here.>
