---
name: task
description: "Capture alias — file a task. A one-word proxy: `/task <thing to build>` records a follow-up through the records instrument's task capture; that verb file owns the whole procedure. Use when the user runs `/task ...` or wants the shortest path to adding a backlog item."
---

# task — capture alias

A proxy skill, nothing more: `/task <args>` invokes `/backlog task <args>` — read and follow
`skills/backlog/verbs/task.md`. Removable without trace (an optional pack member; nothing depends
on it).
