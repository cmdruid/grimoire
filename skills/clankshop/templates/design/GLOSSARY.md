# Glossary

Shared vocabulary for this project's `.handbook/design/` seed. Every spec in `src/` should use these terms
consistently rather than redefining them locally — if a spec needs a term that isn't here yet,
add it here first.

## Terms

<Each entry is one line: a short bold term, an em-dash, and its definition in this project's
context.>

- **<term>** — <definition>
- **<term>** — <definition>
