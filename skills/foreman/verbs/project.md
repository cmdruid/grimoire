# `project` — validate Foreman's projection

Resolve `<root>` and `.agents/skilldata`, then run package-local `scripts/foreman-door.sh check`.
Report the door class, block health, and pointer drift. A healthy route is a no-op.

For drift, preview and apply only Foreman's delimited span using the same authorization rules as
`setup`. Never regenerate foreign publisher blocks, embed a catalog, or create operation/doctrine
directories. Return foreign projection problems as bounded follow-up.
