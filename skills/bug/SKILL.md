---
name: bug
description: "Capture alias — file a bug. A one-word proxy: `/bug <what broke>` records a reproducible defect through the records instrument's bug capture; that verb file owns the whole procedure. Use when the user runs `/bug ...` or wants the shortest path to filing a defect."
---

# bug — capture alias

A proxy skill, nothing more: `/bug <args>` invokes `/backlog bug <args>` — read and follow
`skills/backlog/verbs/bug.md`. Removable without trace (an optional pack member; nothing depends
on it).
